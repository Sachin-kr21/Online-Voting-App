<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %> 
    </title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="./css/styles.css">
</head>
<body>
    <div class="grid grid-cols-6">
        <div class="col-start-3 col-span-2">
            <%- include('header.ejs') %>
            <h2>
                <%=election.name%>
            </h2>
            <br><br>
            <% var questionCount=1 %>
            <% for(let i=0;i<allQuestions.length;i++){  %>
                
                    Question <%= questionCount %> 
                    <h1 class="quesName" ><b><big><%=allQuestions[i].name%></big></b></h1>
                    <div class="quesDesc"> 
                        <%=allQuestions[i].desc%>
                    </div>
                    <%optionCount=1%>
                    <ul class="options">
                        <%for(j in allOptions[allQuestions[i].id]){%>
                        <li>
                            &emsp;
                            <%= optionCount %>. <%=allOptions[allQuestions[i].id][j].name%>
                        </li>
                        <%optionCount++}%>
                    </ul>
                    <br
                <hr style="height:2px;border-width:0;color:gray;background-color:gray">
                <br>
                <% questionCount++; }%>
                <div class="py-3">
                    <button class="text-white bg-green-600 hover:bg-green-800 font-medium rounded px-5 py-2" type="submit">Launch Election</button>
                </div>
        </div>
    </div>
</body>
</html>